# ── Django core ──────────────────────────────────────────────────────────────
django>=5.1,<6.0
django-environ>=0.11,<1.0
django-celery-beat>=2.6,<3.0
psycopg2-binary>=2.9.9,<3.0
whitenoise[brotli]>=6.7,<7.0

# ── Task queue ────────────────────────────────────────────────────────────────
# Pin redis explicitly — celery[redis] leaves it open and the resolver
# backtracks for minutes trying kombu / amqp combinations.
celery[redis]>=5.4,<6.0
redis>=5.0,<6.0

# ── AWS (NOAA public buckets — no credentials needed) ────────────────────────
boto3>=1.34,<2.0
botocore>=1.34,<2.0

# ── Radar processing ─────────────────────────────────────────────────────────
# arm-pyart is the official ARM package; pyart-mch (MeteoSwiss fork) has no
# Windows or Python 3.14 wheels.  Both expose the same pyart.* namespace.
arm-pyart>=2.0,<3.0
metpy>=1.6,<2.0
pyhail>=0.3,<1.0

# pysteps / cfgrib / eccodes have no Windows cp314 wheels and require C
# libraries not available on Windows by default.
# On Windows use requirements/windows-dev.txt which omits these three.
pysteps>=1.8,<2.0
cfgrib>=0.9.14,<1.0
eccodes>=1.6,<2.0

# ── Scientific stack ─────────────────────────────────────────────────────────
numpy>=2.0,<3.0
scipy>=1.13,<2.0
matplotlib>=3.9,<4.0
Pillow>=10.4,<12.0
xarray>=2024.6,<2026.0
